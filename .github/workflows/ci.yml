name: CI

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build-lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test in Docker
        run: |
          docker build -t kukri-ci .
          docker run --rm kukri-ci
